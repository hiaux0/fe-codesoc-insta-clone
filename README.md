# Aurelia 2 default app

This is the default basic app generated by

```
$> npx makes aurelia new-project-name -s typescript,sass
```

To run this example, from a new terminal:

```
$> npm i
$> npm start
```
